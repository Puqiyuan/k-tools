note: 1: running these script sudo no need passwd is required 
2: put this project to $HOME 
3: remeber that ClientAliveInterval and ClientAliveCountMax value in /etc/ssh/sshd_config should be longer, 3600 and 48, namely 48h maybe better.
the timeout=ClientAliveInterval*ClientAliveCountMax
